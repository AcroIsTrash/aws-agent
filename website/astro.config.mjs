import { defineConfig } from 'astro/config';

// Update `site` to your real domain once you have one — it powers the
// RSS feed and absolute URLs. Static output deploys anywhere:
// AWS S3 + CloudFront, GitHub Pages, Netlify, Vercel, etc.
export default defineConfig({
  site: 'https://josephlebrun.com',
});
