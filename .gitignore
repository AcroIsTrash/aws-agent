*.pem
# Windows NTFS alternate-data-stream marker files. The ":" git shows is actually
# a Unicode char (U+F03A), so a literal-colon pattern won't match — glob broadly.
*Zone.Identifier
.env
__pycache__/
*.pyc

# Claude Code local config / agent memory
.claude/

# Terraform
*.tfstate
*.tfstate.backup
.terraform/
